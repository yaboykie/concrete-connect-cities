
{
  "buildCommand": "npm install && npm install vite@latest -g && npm install vite@latest && npx vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "installCommand": "npm install --no-audit --no-fund",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/sitemap.xml",
      "headers": [
        { "key": "Content-Type", "value": "application/xml" },
        { "key": "Cache-Control", "value": "public, max-age=3600" }
      ]
    }
  ],
  "devCommand": "npm install vite@latest -g && npx vite"
}
